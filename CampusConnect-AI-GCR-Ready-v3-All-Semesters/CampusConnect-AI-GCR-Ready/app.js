const $=s=>document.querySelector(s),messages=$("#messages"),input=$("#input"),form=$("#form");let history=[];
function add(text,role){const r=document.createElement("div");r.className="row "+role;if(role==="assistant"){const a=document.createElement("div");a.className="avatar";a.textContent="✦";r.appendChild(a)}const b=document.createElement("div");b.className="bubble";b.textContent=text;r.appendChild(b);messages.appendChild(r);messages.scrollTop=messages.scrollHeight;return b}
async function ask(q){q=q.trim();if(!q)return;$("#welcome")?.remove();add(q,"user");history.push({role:"user",text:q});const r=document.createElement("div");r.className="row assistant";r.innerHTML='<div class="avatar">✦</div><div class="bubble typing"><i></i><i></i><i></i></div>';messages.appendChild(r);messages.scrollTop=messages.scrollHeight;try{const res=await fetch("/api/chat",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({message:q,history:history.slice(-10)})});const data=await res.json();if(!res.ok)throw Error(data.error||"The AI service could not respond.");const b=r.querySelector(".bubble");b.className="bubble";b.textContent=data.reply;history.push({role:"model",text:data.reply})}catch(e){const b=r.querySelector(".bubble");b.className="bubble";b.textContent="⚠️ "+e.message}}
document.addEventListener("click",e=>{const b=e.target.closest("[data-q]");if(b)ask(b.dataset.q)});form.addEventListener("submit",e=>{e.preventDefault();ask(input.value);input.value="";input.style.height="auto"});input.addEventListener("keydown",e=>{if(e.key==="Enter"&&!e.shiftKey){e.preventDefault();form.requestSubmit()}});input.addEventListener("input",()=>{input.style.height="auto";input.style.height=Math.min(input.scrollHeight,130)+"px"});
$("#theme").onclick=()=>{document.body.classList.toggle("dark");$("#theme").textContent=document.body.classList.contains("dark")?"☀":"☾"};
$("#clear").onclick=$("#newChat").onclick=()=>{history=[];location.reload()};
